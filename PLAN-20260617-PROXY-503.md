# PLAN-20260617-PROXY-503

## Tasks

- [ ] TASK-PX-1: 確認容器狀態（3 tool calls）
  - Dev Agent: `docker inspect cont-cont-proxy-1 --format '{{.Created}}'` 取得容器 build 時間
  - Dev Agent: `git log -1 --format='%ci'` 取得 develop 最新 commit 時間
  - 預期完成定義：確認容器是否落後於最新 develop

- [ ] TASK-PX-2: Rebuild cont-proxy（如需）（5~7 tool calls）
  - Dev Agent: docker compose build --pull --no-cache cont-proxy
  - Dev Agent: docker stop cont-proxy && docker rm cont-proxy && docker run -d --name cont-proxy ...
  - Dev Agent: git commit（若有任何 code 變更）
  - Dev Agent: git push origin develop
  - QA Agent: 驗證 `/internal/config/snapshot` targets map 不再是 null
  - 預期完成定義：cont-proxy 容器是最新 build，targets map 是 `[]` 而非 `null`

- [ ] TASK-PX-3: Proxy routing smoke test（4 tool calls）
  - QA Agent: `GET /upstreams/{id}/targets` → 確認有 targets 或 `[]`
  - QA Agent: `GET /test-api/health` via Gateway → 200（regression check）
  - QA Agent: 新建 upstream + target + service + route → `GET /new-route/health` → 200
  - 預期完成定義：所有 proxy routing 測試返回 200，非 502/503
