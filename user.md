# Cont 操作手冊

本手冊說明 Cont API Gateway 管理平台所有頁面的功能與操作方式。

**前置條件：** 以 `admin` 帳號登入管理介面（`http://localhost:18082`）

---

## 登入與帳號管理

### 登入

1. 開啟 `http://localhost:18082`
2. 輸入帳號密碼（預設：`admin` / `admin123`）
3. 支援 Google SSO 登入（Mock）和一般帳號登入

### 帳號角色說明

| 角色 | 權限範圍 |
|------|---------|
| admin | 完整權限，可管理所有資源 |
| editor | 檢視與編輯，無刪除權限 |

---

## 頁面總覽

左側選單分為以下區塊：

- **dashboard** — 儀表板
- **api** — API 服務管理
- **routes** — 路由管理
- **plugins** — 插件管理
- **user** — 消費者管理
- **node-index** — 上游目標管理
- **cloud** — gRPC 服務管理
- **bar-chart** — 統計報告
- **audit** — 審計日誌
- **history** — 設定版本
- **heart** — 服務健康度
- **bell** — 告警規則
- **file-text** — API Key 申請
- **book** — API 文件
- **setting** — 系統設定
- **user** — 使用者管理
- **team** — 群組管理
- **safety** — 工作區管理

---

## 登入頁（Login）

首次開啟系統時顯示。

**功能說明：**
- 一般帳號登入（帳號 + 密碼）
- Google SSO 登入（Mock 模式，點擊後自動登入）
- 展示模式提示：可使用 `admin / admin123` 或 `user / user123` 登入

**操作方式：** 輸入帳密後點擊「Sign In」或直接點擊 Google 登入按鈕。

---

## 儀表板（Dashboard）

**路徑：** `/dashboard`

**功能說明：** 系統總覽頁，展示即時指標。

**畫面內容：**
- 總服務數（Total Services）
- 總路由數（Total Routes）
- 總消費者數（Total Consumers）
- 活躍上游泳數（Active Upstreams）
- API 反應時間趨勢圖
- 各端點請求量分布

**操作方式：** 頁面自動載入，無需操作。右側「刷新」按鈕可重新讀取資料。

---

## API 服務（Services）

**路徑：** `/services`

**功能說明：** 管理 Kong 風格的 API 服務（Service），每個 Service 對應一個上游服務。

**欄位說明：**

| 欄位 | 說明 |
|------|------|
| 名稱 | 服務識別名 |
| URL | 上游目標位址（如 `http://backend:8080`） |
| 協定 | `http` / `https` / `grpc` / `grpcs` |
| 路徑前綴 | 路由比對前綴（選填） |
| 連接逾時 | 單位毫秒 |
| 寫入逾時 | 單位毫秒 |
| 讀取逾時 | 單位毫秒 |
| 啟用 | 是否啟用該服務 |

**操作方式：**
- **新增：** 點擊右上「新增服務」→ 填寫表單 → 儲存
- **編輯：** 點擊操作列「編輯」→ 修改欄位 → 儲存
- **刪除：** 點擊操作列「刪除」→ 確認
- **指派工作區：** 點擊「指派工作區」設定所屬範圍

---

## 路由（Routes）

**路徑：** `/routes`

**功能說明：** 管理路由規則，將客戶端請求導向到指定的 Service。

**欄位說明：**

| 欄位 | 說明 |
|------|------|
| 名稱 | 路由識別名 |
| 服務 | 關聯的 Service |
| 路徑 | 匹配路徑（如 `/api/`） |
| 主機名 | 匹配 Host 頭（如 `api.example.com`） |
| 協定 | `http` / `https` / `grpc` |
| Strip Path | 是否去除匹配前綴後轉發 |
|  Preserve Host | 是否保留原始 Host |
| 啟用 | 是否啟用該路由 |

**操作方式：**
- **新增：** 點擊「新增路由」→ 選擇關聯 Service → 設定匹配規則 → 儲存
- **編輯：** 點擊操作列「編輯」→ 修改設定 → 儲存
- **刪除：** 點擊操作列「刪除」→ 確認

---

## 插件（Plugins）

**路徑：** `/plugins`

**功能說明：** 設定 API 攔截器，可在請求/回應階段修改行為。

**支援的插件類型：**

| 插件名稱 | 功能說明 |
|---------|---------|
| rate-limiting | 請求速率限制（分鐘/小時/日） |
| proxy-cache | 回應快取 |
| cors | 跨域資源共享設定 |
| response-transformer | 修改回應 Header / JSON 屬性 |
| correlation-id | 請求追蹤 ID |
| jwt | JWT 身份驗證 |
| key-auth | API Key 驗證 |
| basic-auth | Basic 帳密驗證 |
| oauth2 | OAuth 2.0 授權 |
| acl | 存取控制名單 |
| ip-restriction | IP 黑/白名單 |
| request-size-limiting | 請求體積限制 |
| response-rate-limiting | 回應速率限制 |

**操作方式：**
- **新增：** 點擊「新增插件」→ 選擇插件類型 → 設定參數 → 儲存
- **套用範圍：** 可設定「全域」或指定「Service / Route / Consumer」
- **參數範例（rate-limiting）：**
  - `minute`：每分鐘最大請求數
  - `hour`：每小時最大請求數
  - `day`：每日最大請求數
  - `policy`：限速演算法（`local` / `redis`）
- **編輯：** 點擊操作列「編輯」→ 修改參數 → 儲存
- **刪除：** 點擊操作列「刪除」→ 確認

---

## 消費者（Consumers）

**路徑：** `/consumers`

**功能說明：** 管理 API 消費者（end-user 或呼叫端），用於綁定插件（限速、驗證等）。

**欄位說明：**

| 欄位 | 說明 |
|------|------|
| 使用者名稱 | 消費者唯一識別 ID |
| 自訂 ID | 自訂數值 ID（選填） |
| 啟用 | 是否啟用該消費者 |

**操作方式：**
- **新增：** 點擊「新增消費者」→ 填寫資料 → 儲存
- **編輯：** 點擊操作列「編輯」→ 修改資料 → 儲存
- **刪除：** 點擊操作列「刪除」→ 確認
- **檢視凭證：** 點擊「View Credentials」可查看該消費者的 JWT / Key-auth 凭證

---

## 上游（Upstreams）

**路徑：** `/upstreams`

**功能說明：** 管理上游目標叢集（Target），支援負載平衡與健康檢查。

**欄位說明：**

| 欄位 | 說明 |
|------|------|
| 名稱 | 上游識別名 |
| 類型 | 負載平衡類型（`roundrobin` / `least-connections`） |
| 演算法 | 權重分配（`consistent-hashing` 可選） |
| 健康檢查閾值 |  healthy / unhealthy 閾值 |

**Targets（目標）：**

| 欄位 | 說明 |
|------|------|
| 目標位址 | IP 或 hostname（如 `192.168.1.10:8080`） |
|連接埠 | 目標通訊埠 |
| 權重 | 流量權重（預設 100） |
| 啟用 | 是否接受流量 |

**操作方式：**
- **新增上游：** 點擊「新增上游」→ 填寫名稱與類型 → 儲存
- **新增目標：** 在上游詳情頁點擊「新增目標」→ 填寫 IP/Port/權重 → 儲存
- **編輯：** 點擊操作列「編輯」
- **刪除：** 點擊操作列「刪除」→ 確認

---

## gRPC 服務（Grpc Services）

**路徑：** `/grpc`

**功能說明：** 管理 gRPC 服務，與 Services 頁面類似但專門用於 gRPC 協定。

**欄位說明：**

| 欄位 | 說明 |
|------|------|
| 名稱 | gRPC 服務名 |
| URL | gRPC 上游位址（如 `grpc://localhost:50051`） |
| 協定 | `grpc` 或 `grpcs` |
| TLS 設定 | 是否啟用 TLS |

**操作方式：** 與 Services 頁面相同（新增/編輯/刪除）。

---

## 統計報告（Analytics）

**路徑：** `/analytics`

**功能說明：** 展示 API 使用統計數據與趨勢。

**畫面內容：**
- 請求量趨勢圖（可選時間範圍：24小時 / 7天 / 30天）
- 各端點使用量排行榜
- 平均回應時間
- 錯誤率分布
- 流量來源分布（可依 Consumer / Service / Route 篩選）

**操作方式：**
- 使用頂部時間範圍切換器切換顯示區間
- 使用篩選器依 Service / Route / Consumer 細分資料
- 點擊「刷新」重新讀取

---

## 審計日誌（Audit Log）

**路徑：** `/audit`

**功能說明：** 記錄所有管理操作，留存合規與問題排查用。

**欄位說明：**

| 欄位 | 說明 |
|------|------|
| 時間 | 操作時間 |
| 帳號 | 執行操作的帳號 |
| 動作 | 操作類型（`CREATE` / `UPDATE` / `DELETE`） |
| 資源類型 | 目標資源（Service / Route / Consumer 等） |
| 資源名稱 | 目標資源名稱 |
| 詳情 | 操作細節（JSON格式） |
| IP 位址 | 客戶端 IP |

**操作方式：**
- 支援時間範圍篩選
- 支援帳號/資源類型/動作篩選
- 點擊列可展開詳情（JSON 格式）

---

## 設定版本（Config Versioning）

**路徑：** `/history`

**功能說明：** 管理設定快照版本，可隨時還原至先前狀態。

**畫面內容：**
- 版本列表（時間、版本號、變動摘要）
- 每次儲存自動產生新版本

**操作方式：**
- **檢視版本：** 點擊列表項目查看該版本的完整設定
- **還原：** 點擊「還原至此版本」→ 確認（會建立新版本再套用）
- **比較：** 點擊「比較」可對比兩個版本的差異

---

## 服務健康度（Health Portal）

**路徑：** `/health`

**功能說明：** 展示所有 upstream targets 的健康狀態。

**狀態燄號：**

| 狀態 | 燄號顏色 | 說明 |
|------|---------|------|
| Healthy | 綠色 | 目標正常，接收流量 |
| Unhealthy | 紅色 | 目標異常，暫停轉發 |
| DNS Error | 灰色 | DNS 解析失敗 |

**畫面內容：**
- 各 Upstream 的 Targets 健康狀態列表
- 健康檢查設定（頻率、逾時、閾值）
- 最後健康檢查時間

**操作方式：**
- 點擊上游名稱查看其下所有 Targets
- 支援手動開關 Target（啟用/停用）
- 可設定健康檢查參數

---

## 告警規則（Alert Rules）

**路徑：** `/alerts`

**功能說明：** 設定指標閾值告警，達到條件時觸發通知。

**支援的告警條件：**

| 條件 | 說明 |
|------|------|
| 錯誤率過高 | 5xx 比例超過設定值 |
| 回應時間過長 | P99延遲超過設定值（毫秒） |
| 上游離線 | 目標被標記為 unhealthy |
| 流量異常 | 請求量低於或高於歷史平均值 % 數 |
| 連線數過高 | 同時連線數超過設定值 |

**操作方式：**
- **新增規則：** 點擊「新增規則」→ 選擇監控類型 → 設定閾值 → 選擇通知方式 → 儲存
- **編輯：** 點擊操作列「編輯」→ 修改參數 → 儲存
- **刪除：** 點擊操作列「刪除」→ 確認
- **啟用/停用：** 使用開關快速切換規則狀態

**通知方式：** 目前支援頁面內通知，Webhook 通知（需設定）。

---

## API Key 申請（API Key Requests）

**路徑：** `/api-keys`

**功能說明：** 管理 API Key 申請單，審核通過後產生可用 Key。

**欄位說明：**

| 欄位 | 說明 |
|------|------|
| 申請者 | 申請人帳號 |
| 申請 Key 數量 | 申請的 Key 數量 |
| 預計用量 | 日/週/月 用量估算 |
| 申請時間 | 送出時間 |
| 狀態 | `pending`（待審）/ `approved`（已核准）/ `rejected`（已拒絕） |

**操作方式：**
- **審核：** 點擊「Approve」或「Reject」按鈕
- **已核准 Key：** 審核通過後產生的 Key 會顯示在列表中，複製後提供給申請者

---

## API 文件（API Docs）

**路徑：** `/api-docs`

**功能說明：** 展示 OpenAPI（Swagger）互動式 API 文件。

**功能說明：**
- 可直接在頁面上嘗試（Try it out）各個 API 端點
- 支援 JWT / API Key 授權
- 顯示完整請求/回應格式與範例

**操作方式：**
- 點擊任意端點展開詳情
- 點擊「Try it out」輸入參數後點「Execute」測試
- 支援篩選（Filter）快速找到所需端點

---

## 系統設定（Settings）

**路徑：** `/settings`

**功能說明：** 全域系統設定，包含外觀與通知設定。

**設定項目：**

| 設定 | 說明 |
|------|------|
| 站台名稱 | 系統顯示名稱 |
| 站台 Logo | 上傳品牌標誌 |
| 佈景主題 | `dark`（預設）/ `light` |
| 預設工作區 | 新使用者預設所屬工作區 |
| 時區設定 | 時間顯示時區 |
| 通知方式 | Email / Slack / Webhook |
| SMTP 設定 | 寄件伺服器設定（Host / Port / 帳密） |
| Webhook URL | 告警與事件通知目標 |

**操作方式：**
- 直接修改欄位後點擊「儲存」
- 測試通知：修改後點擊「傳送測試通知」驗證設定是否正確

---

## 使用者管理（Users）

**路徑：** `/users`

**功能說明：** 管理系統帳號（與 Kong Admin 帳號分開，屬於 Cont 平台帳號）。

**欄位說明：**

| 欄位 | 說明 |
|------|------|
| 使用者名稱 | 登入帳號 |
| 顯示名稱 | 畫面顯示名稱 |
| Email | 聯絡信箱 |
| 角色 | `admin` / `editor` |
| 啟用 | 帳號是否啟用 |
| 群組 | 所屬群組 |
| 建立時間 | 帳號建立時間 |

**操作方式：**
- **新增使用者：** 點擊「新增使用者」→ 填寫資料 → 設定角色 → 儲存
- **編輯：** 點擊操作列「編輯」→ 修改資料 → 儲存
- **刪除：** 點擊操作列「刪除」→ 確認（無法刪除自己）
- **指派工作區：** 點擊「指派工作區」設定可存取的工作區範圍
- **指派群組：** 點擊「指派群組」設定所屬群組
- **資源權限：** 點擊「資源權限」設定該使用者對各資源的存取權限

---

## 群組管理（Groups）

**路徑：** `/groups`

**功能說明：** 將多個使用者歸組，方便批次授權與管理。

**欄位說明：**

| 欄位 | 說明 |
|------|------|
| 群組名稱 | 群組唯一識別名 |
| 標籤 | 顯示標籤（如「QA」、「開發」） |
| 說明 | 群組用途描述 |
| 權限數 | 該群組已指派的權限規則數 |
| 建立時間 | 建立時間 |

**操作方式：**
- **新增群組：** 點擊「新增群組」→ 填寫名稱與說明 → 儲存
- **編輯：** 點擊操作列「編輯」→ 修改資料 → 儲存
- **刪除：** 點擊操作列「刪除」→ 確認
- **指派成員：** 在群組詳情頁可新增/移除成員使用者

---

## 工作區管理（Workspaces）

**路徑：** `/workspaces`

**功能說明：** 隔離不同專案/部門的資源，支援 Resource-level RBAC。

**欄位說明：**

| 欄位 | 說明 |
|------|------|
| 工作區名稱 | 隔離空間名稱 |
| 說明 | 用途描述 |
| 資源數 | 歸屬於該工作區的資源數 |

**操作方式：**
- **新增工作區：** 點擊「新增工作區」→ 填寫資料 → 儲存
- **編輯：** 點擊操作列「編輯」→ 修改資料 → 儲存
- **刪除：** 點擊操作列「刪除」→ 確認（需先移除所有資源）
- **詳情：** 點擊工作區名稱進入詳情頁，可檢視/管理該工作區內的所有資源

---

## Webhook 事件（Webhook Deliveries）

**路徑：** `/webhooks`

**功能說明：** 檢視所有 Webhook 事件傳送歷史與狀態。

**欄位說明：**

| 欄位 | 說明 |
|------|------|
| 事件類型 | 觸發的事件類型 |
| 目標 URL | Webhook 接收端點 |
| 傳送時間 | 嘗試時間 |
| 狀態 | `success` / `failed` / `pending` |
| 回應狀態碼 | HTTP 回應碼 |
| 重試次數 | 失敗後重試次數 |

**操作方式：**
- 點擊列可展開查看完整 Request / Response 內容
- 可手動「重試」失敗的傳送
- 支援時間範圍與狀態篩選

---

## 告警歷史（Alert History）

**路徑：** `/alert-history`

**功能說明：** 查詢所有已觸發的告警事件歷史。

**欄位說明：**

| 欄位 | 說明 |
|------|------|
| 觸發時間 | 告警發生時間 |
| 告警規則 | 觸發的規則名稱 |
| 嚴重程度 | `critical` / `warning` / `info` |
| 狀態 | `firing`（觸發中）/ `resolved`（已解決） |
| 詳情 | 觸發時的指標數值 |

**操作方式：**
- 支援時間範圍與嚴重程度篩選
- 點擊列展開詳情
- 可將告警標記為已處理

---

## 登出

**操作方式：** 點擊右上角「登出」按鈕即可登出目前帳號。
