# SPEC-20260617-PROXY-503

## 🔴 P0 Bug — Proxy 轉發返回 503（upstream targets nil）

## 背景
- **發現時間**: 2026-06-16 18:51 UTC
- **小黑驗證（2026-06-17 14:22 QA Run #3）**:
  - Upstream `192.168.1.202:3010` 存在
  - `GET /upstreams/{id}/targets` 返回 `{"data":null,"next":""}` → targets 未初始化
  - `GET /{route_path}/health` via Gateway → 503 `{"message":"no upstream target"}`
- **小黑分析（2026-06-17 14:30）**:
  - Go targetsMap init 已存在（`routes/routes.go` line 1452：`targetsMap[u.ID] = []ProxyTarget{}`）
  - Lua type guard 已存在（`nginx.conf` line 567：`type(cont.targets[svc.upstream_id]) ~= "nil"`）
  - **問題可能是容器未 rebuild 最新 develop**，或 upstream/target 狀態不一致

## 小黑確認（14:35）

### Go fix 已存在 ✅
```go
// routes/routes.go:1452
targetsMap[u.ID] = []ProxyTarget{} // init empty
```

### Lua fix 已存在 ✅
```lua
-- nginx.conf:567
if service.upstream_id and cont.upstreams and cont.upstreams[service.upstream_id]
   and cont.targets and type(cont.targets[service.upstream_id]) ~= "nil"
   and next(cont.targets[service.upstream_id]) then
```

### 問題分析
若 Go + Lua fix 都已存在但仍然 503，則可能是：
1. **容器未 rebuild**：舊 container 仍用舊 code
2. **Upstream 有 targets 但 snapshot API 回傳 null**：snapshot API 有 bug
3. **Upstream 根本沒有 targets**：需手動新增 target

### 待確認
1. 確認容器 build 時間：`docker inspect cont-cont-proxy-1 --format '{{.Created}}'`
2. 確認 develop branch 最新 commit時間
3. 若容器落後，需 rebuild；若容器最新但仍失敗，則需进一步分析

## 目標
確認 Proxy 503 根因並修復：
- 新建 Service（via upstream）+ Route → `GET /qa_fwd_*/health` → 200
- 現有 `/test-api/health` → 200（regression check）

## Scope

### In-Scope
- 確認 cont-proxy 容器是否落後（build date vs latest develop commit）
- 若容器落後：rebuild cont-proxy with --no-cache
- 若容器最新：分析 snapshot API 為何回傳 null
- 驗證 `GET /internal/config/snapshot` 的 targets map

### Out-of-Scope
- 不改動 Admin API 其它邏輯
- 不改動 upstream/target store 邏輯

## 驗收標準
1. `GET /internal/config/snapshot` targets map 中所有 upstream_id 都對應陣列（`[]` 而非 `null`）
2. 新建 Service（via upstream_id）+ Route → `GET /qa_fwd_*/health` → 200（非 502/503）
3. 現有 `/test-api/health` → 200（regression check）
4. Docker build --no-cache cont-proxy 成功（若需 rebuild）
5. 容器 healthy
